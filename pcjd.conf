{
    hypnotoad => {
        heartbeat_timeout  => 120,  # 2 minutes
        inactivity_timeout => 120,  # 2 minutes
        listen             => ['http://*:5555'],
        workers            => 6,
    },

    perlcrtitic => {
        -theme   => 'core',
        -exclude => [ qw(PerlTidy PodSpelling) ],
    },

    google_analytics_id     => 'UA-10783012-1',
};
